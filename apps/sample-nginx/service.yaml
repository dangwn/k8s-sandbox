apiVersion: v1
kind: Service
metadata:
  name: sample-nginx
  namespace: default
  labels:
    app: sample-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: sample-nginx