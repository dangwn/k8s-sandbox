apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: ${APPS_INTERVAL}
  path: ${APPS_PATH}
  prune: ${PRUNE_ENABLED}
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: flux-system